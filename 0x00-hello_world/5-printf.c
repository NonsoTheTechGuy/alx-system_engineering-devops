#include <stdio.h>

/**
 * main - A program that prints a line with printf function
 *return: it should end with 0
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n"); 
return (0);
}
